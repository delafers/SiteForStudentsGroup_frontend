import React from 'react';
import Style from './News.module.css';

const News = (props) => {
    return (
        <div>
            news
            {console.log(props)}
        </div>
    );
};

export default News;